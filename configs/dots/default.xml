<?xml version="1.0" encoding="utf-8"?>
<!-- This file is the default file used for config dots. 
Each config dots file must have this option, no matter if you use them or not.
Always default.xml will be loaded, and it's value can be overwritten by the 
current xml dots file. -->
<config>
	<body>
	<variable option="global">
	</variable>
	<variable option="admin">
	</variable>
	<variable option="frontend">
		<warningMessage>
			<userPermission>You don't have enough credentials to access this url.</userPermission>
		</warningMessage>
	</variable>		
	</body>
</config>